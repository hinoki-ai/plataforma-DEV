#!/usr/bin/env bash
# Quick local dev server shortcut
# Usage: zz

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$PROJECT_DIR" || exit 1

echo "🚀 Starting local development server..."
npm run dev

