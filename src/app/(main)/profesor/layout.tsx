import ClientProfesorLayout from "@/components/layout/ClientProfesorLayout";

export default function ProfesorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <ClientProfesorLayout>{children}</ClientProfesorLayout>;
}
